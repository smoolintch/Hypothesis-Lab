# contracts

这里用于记录系统契约，重点是“字段定义”和“模块间共同语言”。

建议后续优先补充：

1. `StrategyCard` 字段定义
2. `BacktestRequest` 输入结构
3. `BacktestResult` 返回结构
4. `Conclusion` 与 `HandbookEntry` 关联方式
5. 错误码与异常返回格式
6. `RuleTemplate` 参数 schema 与标准化输出结构

## 当前核心文档
1. `domain-model.md`
2. `api-contracts.md`
3. `rule-template-schema-v1.md`
